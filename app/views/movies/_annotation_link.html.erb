<%=
    icon_name = "#{ annotation }#{ '_unchecked' if ! value }.png"
    path_with_annotation = movie_path(movie, annotation => ! value)

    link_to image_tag( icon_name, size: "16" ), path_with_annotation, method: :put, remote: true
%>